{{ form_start(form, {'method': 'post', 'action': path('user_register'), 'attr': {'class': 'user_register'}}) }}
    Email : {{ form_widget(form.email) }}
    Pseudo : {{ form_widget(form.username) }}
    Mot de passe : {{ form_widget(form.plainPassword.first) }}
    Confirmation du mot de passe : {{ form_widget(form.plainPassword.second) }}
<div>
    <input type="submit" value="{{ 'registration.submit'|trans }}" />
</div>
{{ form_end(form) }}
